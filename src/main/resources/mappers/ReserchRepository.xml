<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.ReserchRepository">

	<select id="articleReerchId"
		resultType="com.example.demo.vo.Article">
		SELECT A.id
		FROM article A
		INNER JOIN board B
		ON A.boardId = B.id
		INNER JOIN `member` M
		ON A.memberId = M.id
		WHERE A.title LIKE '%#{keyword}%'
		OR M.nickname LIKE '%#{keyword]%'
	</select>



</mapper>